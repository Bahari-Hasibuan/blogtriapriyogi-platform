export default function EditorPage() {
  return (
    <main style={{ minHeight: "100vh", padding: 24, background: "#f7f5ff" }}>
      <nav style={{ display: "flex", justifyContent: "space-between", marginBottom: 28 }}>
        <a href="/dashboard" style={{ fontWeight: 900 }}>← Dashboard</a>
        <a href="/">Lihat Website</a>
      </nav>

      <h1 style={{ fontSize: 42 }}>Editor Artikel</h1>
      <p style={{ color: "#6f6b80" }}>Tulis artikel, simpan draft, atau publish.</p>

      <section style={{ background: "white", padding: 24, borderRadius: 24, marginTop: 24 }}>
        <input placeholder="Judul artikel" style={{ width: "100%", padding: 18, fontSize: 24, border: "1px solid #e5e0ff", borderRadius: 16, marginBottom: 16 }} />

        <textarea placeholder="Tulis isi artikel di sini..." style={{ width: "100%", minHeight: 320, padding: 18, border: "1px solid #e5e0ff", borderRadius: 16 }} />

        <div style={{ display: "flex", gap: 12, marginTop: 18 }}>
          <button style={{ padding: 14, border: 0, borderRadius: 14, background: "#eeeaff", color: "#6d28d9", fontWeight: 900 }}>Generate AI</button>
          <button style={{ padding: 14, border: 0, borderRadius: 14, background: "#171427", color: "white", fontWeight: 900 }}>Simpan Draft</button>
          <button style={{ padding: 14, border: 0, borderRadius: 14, background: "#7c3aed", color: "white", fontWeight: 900 }}>Publish</button>
        </div>
      </section>
    </main>
  );
}
