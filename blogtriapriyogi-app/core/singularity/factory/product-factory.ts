export function createProduct(name: string) {
  return {
    name,
    frontend: "generated UI system",
    backend: "auto API system",
    database: "auto schema",
    deployment: "instant cloud deploy",
    status: "live-ready"
  }
}
